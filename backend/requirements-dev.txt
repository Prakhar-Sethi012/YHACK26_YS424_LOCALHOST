-r requirements.txt
pytest>=8.3
pytest-asyncio>=0.24
# httpx is a runtime dep now too (see requirements.txt) -- kept implicit here
# via -r above rather than re-pinned, to avoid two sources of truth.

# backend/test_client.py (manual WebSocket smoke-test script for server.py)
websockets>=13.0
